package entity.animal.herbivore;

import entity.animal.Animal;
import config.AnimalSpecies;

public class Herbivore extends Animal {

    protected Herbivore(AnimalSpecies species) {
        super(species);
    }
}
