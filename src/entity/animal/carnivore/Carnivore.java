package entity.animal.carnivore;

import entity.animal.Animal;
import config.AnimalSpecies;

public class Carnivore extends Animal {

    protected Carnivore(AnimalSpecies species) {
        super(species);
    }
}
